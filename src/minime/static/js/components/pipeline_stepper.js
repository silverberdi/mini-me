export const PIPELINE_STAGES = ["WORKTREE_SETUP", "IMPLEMENTATION", "CHECKS", "REVIEW", "AUDIT", "PREVIEW", "PR", "MERGE_GATE", "CLOSURE"];
export function renderPipelineStepper(container, current, state = {}) { if (!container) return; const active = PIPELINE_STAGES.indexOf(current); container.className = "pipeline-nine-stage"; container.innerHTML = PIPELINE_STAGES.map((stage, index) => { const status = state[stage] || (index < active ? "complete" : index === active ? "active" : "pending"); return `<div class="step is-${status}" data-stage="${stage}" aria-current="${status === "active" ? "step" : "false"}"><span>${index + 1}</span><br>${stage.replaceAll("_", " ")}</div>`; }).join(""); }
