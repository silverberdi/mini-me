export function confirmAction(action, onConfirm) { const dialog = document.querySelector("#actionDialog"); if (!dialog) return onConfirm(); dialog.querySelector("[data-dialog-title]").textContent = action.display_name; dialog.querySelector("[data-dialog-description]").textContent = action.confirmation_prompt || action.description; dialog.showModal(); dialog.querySelector("[data-confirm]").onclick = () => { dialog.close(); onConfirm(); }; dialog.querySelector("[data-cancel]").onclick = () => dialog.close(); }
